# Dependencies (ไม่ต้องเอาขึ้น เพราะเราจะ npm install ใหม่ได้)
node_modules/
**/node_modules/

# Environment Variables (ความลับ ห้ามเอาขึ้นเด็ดขาด!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
backend/.env
frontend/.env
**/secret/

# Build Outputs (ไฟล์ที่คอมไพล์แล้ว ไม่ต้องเอาขึ้น)
build/
dist/
out/

# IDE & System Files (ไฟล์ตั้งค่าส่วนตัวของเครื่องเรา)
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# Certs (ถ้ามี key SSL)
*.key
*.crt
*.pem

mongodb_data/
*.lock